# Causal Inference Final Project

Replication and extension of Sánchez-García, Rodon & Delgado-García (2025),
*Where has everyone gone? Depopulation and voting behaviour in Spain*,
*European Journal of Political Research*, 64, 296–319.
DOI: [10.1111/1475-6765.12702](https://doi.org/10.1111/1475-6765.12702).

Author: Giorgio Coppola. Hertie School, Causal Inference (Spring 2026).

[**Read the report here →**](https://raw.githack.com/giocopp/SRD-replication-extension/main/final_project.html)

## What's in the project

- **Paper summary** — research question, theoretical and empirical estimand,
  DAG, identification assumptions and challenges.
- **Replication** — Table 2 of the paper (controlled two-way fixed-effects
  estimates of depopulation on vote shares for PSOE, PP, UP, Cs, PANES, ES,
  Vox, 2011–2019), reproduced from the authors' `analysis_manuscript.R`.
- **Extension** — a `grf` causal forest on Vox vote share to test for
  heterogeneous treatment effects across municipalities.

## File layout

```
final_project.qmd              # main document (source)
final_project.html             # rendered HTML
final_project.pdf              # rendered PDF
custom.scss                    # HTML theme
final_proj_outline.pdf         # assignment brief

replication/
  replicate_table2.R           # Table 2 replication
  extension_causal_forest.R    # causal-forest extension (fits the forest,
                               #   writes tables/figures to outcomes/)
  analysis_manuscript.R        # symlink to authors' original code
  analysis_appendix.R          # symlink to authors' appendix code
  database/                    # symlinks to data files (see Data below)
  outcomes/                    # script outputs consumed by the QMD

final_project_files/           # figures generated by Quarto
```

`data_explore/` is *not* tracked in the repo (see the Data section below).
The pre-rendered `final_project.html` and `final_project.pdf` are committed,
so you can read the document without re-running anything.

## Data

The authors' replication package is not redistributed here. To re-run the
scripts or re-render the QMD you need to obtain it yourself.

- The article is at DOI [10.1111/1475-6765.12702](https://doi.org/10.1111/1475-6765.12702);
  its data-availability statement points to the authors' replication archive.
- Once downloaded, extract the package contents into `data_explore/sanchez_garcia/`
  (create the folder if needed). The symlinks under `replication/database/` and
  `replication/analysis_*.R` point into that directory and will resolve once
  the files are in place.

The files used by the scripts in this repo are: `short_time.RData`,
`long_time.RData`, `manifestos_project.csv`, `nov11.sav`, `dec15.sav`,
`jun16.sav`, `apr19.sav`, `nov19.sav`, and the four
`mapa_municipios_ESPCAN.{dbf,prj,shp,shx}` shapefile components.

## How to reproduce

Run the two scripts, then render the QMD.

```bash
cd replication
Rscript replicate_table2.R
Rscript extension_causal_forest.R
cd ..
quarto render final_project.qmd
```

Requires R and Quarto (and TinyTeX for the PDF: `quarto install tinytex`).
